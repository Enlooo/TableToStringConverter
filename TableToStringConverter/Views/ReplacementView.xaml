<UserControl x:Class="TableToStringConverter.Views.ReplacementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TableToStringConverter.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Horizontal" Background="LightSkyBlue">
        <Label>Text to replace</Label>
        <TextBox Text="{Binding ReplacementKey}" MinWidth="100" Margin="3"/>
        <Label>Column index</Label>
        <TextBox Text="{Binding ColumnIndex}" MinWidth="100" Margin="3"/>
        <Label>Split on key</Label>
        <TextBox Text="{Binding SplitOn}" MinWidth="100" Margin="3"/> 
    </StackPanel>
</UserControl>
